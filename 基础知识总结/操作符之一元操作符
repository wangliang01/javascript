1.表达式

原始表达式：

初始化表达式： 对象初始化表达式， 数组初始化表达式

函数定义表达式：

函数调用表达式：

属性访问表达式：

对象创建表达式：


2.一元加(+),一元减(-)操作符

一元加：将数据类型转换成数值

一元减：先将其转换成数值，再取反



3.递增(++),递减(--)操作符

递增，递减操作符，分前置和后置

前置：先递增，再运算

后置：先运算，再递增


4.按位非(~)

记住一点：按位非的结果就是将值取反再减1

例子： ~10   先将10取反得到-10，再减1得到-11



5.逻辑非(!)

将任意数据类型转成布尔值，再取反，返回值是布尔值

跟逻辑非使用的数据类型能够返回true，只有6个值，undefined,null,0,-0,NaN,''

!! 将任意数据类型转成布尔值



6.typeof

typeof是一个一元操作符，用于检测数据类型，返回值是一个字符串，只有6种返回值

'undefined', 'number', 'boolean', 'string', 'object', 'function'



7.void

void 也是一个一元操作符，其后可以跟任意数据类型， 返回undefined


8.delete

delete也是一个一元操作符，用于删除对象上的一个属性


